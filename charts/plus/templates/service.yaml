apiVersion: v1
kind: Service
metadata:
  name: {{ include "plus.fullname" . }}
  labels:
    {{- include "plus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "plus.selectorLabels" . | nindent 4 }}
