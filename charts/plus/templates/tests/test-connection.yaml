apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plus.fullname" . }}-test-connection"
  labels:
    {{- include "plus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
